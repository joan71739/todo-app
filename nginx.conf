server {
    listen 80;
    server_name localhost;
    
    # React build 的檔案
    root /usr/share/nginx/html;
    index index.html;
    
    # 處理 React Router - 所有路由都返回 index.html
    location / {
        try_files $uri /index.html;
    }
    
    # 移除 /api 代理部分
}